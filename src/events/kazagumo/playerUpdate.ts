import { KazagumoPlayer } from 'kazagumo';

export default {
    name: 'playerUpdate',
    async execute(player: KazagumoPlayer) {
        // Position updates handled internally by Kazagumo.
    },
};
